const navbarElement = document.querySelector("#navbar");
const titleSection = document.querySelector("#titleSection");

// log top bounding rect of title section
console.log(titleSection.getBoundingClientRect().top);
